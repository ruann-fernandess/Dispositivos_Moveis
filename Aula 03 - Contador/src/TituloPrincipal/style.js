import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tituloPrincipal: {
    fontSize: 25,
    fontWeight: 'bold',
    color: 'red',
    marginBottom: 20
  }
});

export { styles }